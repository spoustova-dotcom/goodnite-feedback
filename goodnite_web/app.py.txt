import streamlit as st
import pandas as pd
import plotly.express as px

st.set_page_config(page_title="Goodnite Feedback Hub", layout="wide")

st.title("🌙 Goodnite Feedback Hub")

# Načtení dat
try:
    links_df = pd.read_csv('apartments_links_clean.csv')
    st.success("Data o apartmánech načtena!")
    
    selected_apt = st.selectbox("Vyberte objekt:", ["Všechny"] + list(links_df['Apartmán'].unique()))
    
    st.write(f"Zvolený objekt: {selected_apt}")
    st.info("Zde se budou zobrazovat recenze, jakmile robot dokončí scraping.")
    
    with st.expander("Zobrazit seznam sledovaných odkazů"):
        st.dataframe(links_df)
except:
    st.error("Soubor apartments_links_clean.csv nebyl nalezen.")